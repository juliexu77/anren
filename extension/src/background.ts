/**
 * ANREN extension service worker — replace with your logic.
 * Use shared: import { createSupabaseClient } from "../../shared";
 */

chrome.runtime.onInstalled.addListener(() => {
  console.log("ANREN extension installed.");
});
